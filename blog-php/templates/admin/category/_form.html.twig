{{ form_start(form) }}

    <div class="form-group">
        {{form_row( form.name, {
            'attr' : {
                'class' : 'form-control'
            }
        } )}}
    </div>
    <button class="btn btn-danger float-right">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
